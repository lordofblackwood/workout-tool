# Optional Powerlevel integration

Enter the same private connection code used by the Powerlevel dashboard. History is normalized, encrypted with AES-256-GCM and synced while the tracker is open. The widget never writes the tracker's state or changes progression. Pairing is optional. Use one installation of each tracker per code. On iPhone, pair inside the existing installed app to retain its original history.
